import type { ContactContent } from "@/content/types";

export const contact: ContactContent = {
  replyWindow: "[REPLY WINDOW, e.g. one business day]",
  whatHappensNext: [
    "We read it the same day.",
    "You get a reply within [REPLY WINDOW] with an honest read.",
    "If we're not a fit, we say so and point you somewhere useful.",
  ],
};
