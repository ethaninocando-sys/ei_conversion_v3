import type { ContactContent } from "@/content/types";

export const contact: ContactContent = {
  replyWindow: "[REPLY WINDOW, e.g. one business day]",
  whatHappensNext: [
    "Your message lands in a real inbox, not a queue. We read it the day it arrives.",
    "You get a written answer within [REPLY WINDOW] about whether this is worth either of our time.",
    "If it isn't, we say so and point you at whoever is a better call.",
  ],
};
